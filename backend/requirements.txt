"""Requirements file for the backend.

The backend uses:
- fastapi
- uvicorn[standard]
- sqlalchemy[asyncio]
- asyncpg
- paho‑mqtt
- python‑mypy‑plugins (optional for type checking)
"""

from __future__ import annotations

requirements = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.29.0",
    "paho-mqtt>=2.0.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]

# Write to a requirements.txt file
with open("/home/yutw/project/md_templates/results/smartfarm_manager_flutter/backend/requirements.txt", "w") as f:
    f.write("\n".join(requirements))

print("requirements.txt generated")
